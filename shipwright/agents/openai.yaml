interface:
  display_name: "Shipwright"
  short_description: "Delegate, review, and iterate in persistent panes"
  default_prompt: "Use $shipwright to delegate this implementation in a persistent multiplexer, review it, and bring it to me only when ready."
