interface:
  display_name: "Shipwright"
  short_description: "Launch or attach to agents, then review their work"
  default_prompt: "Use $shipwright to launch or attach to my coding agent in a persistent multiplexer, supervise and review it, and bring the work to me only when ready."
